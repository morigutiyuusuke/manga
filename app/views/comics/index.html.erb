 <div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
          <h2>ユーザー情報</h2>
          <% if @comic.errors.any? %>
          <%= @comic.errors.count %> error prohibited this obj from being saved:
          <% @comic.errors.full_messages.each do |message| %>
          <%= message %>
          <% end %>
          <% end %>
          <%= render 'users/info', user: current_user %>
           <h2 class="mt-3">新規投稿</h2>
            <%= render 'form', comic: @comic %>
    </div>
      <div class='col-md-8 offset-md-1'>
            <h2>漫画</h2>
            <%= render 'index', comics: @comics%>
      </div>
  </div>
 </div>
 <div class="d-flex justify-content-center"><%= paginate @comics %></div>
